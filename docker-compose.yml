version: '3.9'

services:
  backend:
    build: ./backend
    container_name: senior_project_backend
    ports:
      - "5000:5000"
    volumes:
      - ./backend:/app
    command: python NLP_tests/bert_tests.py
